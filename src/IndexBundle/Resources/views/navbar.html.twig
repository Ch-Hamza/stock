<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('index_page') }}">Vente</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('index_products_page') }}">
                    Produits
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('index_categories_page') }}">
                    Catégories
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('index_sales_page') }}">
                    Archive
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('bilan_sale_page') }}">
                    Bilan
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('index_total_page') }}">
                    Total
                </a>
            </li>
            {% if prod_number>0 %}
                <li class="nav-item">
                    <a class="nav-link cart-logo" href="{{ path('missing_product_page') }}">
                        <i class="icon-warning22"></i>
                        <span class="badge badge-pill badge-dark">{{ prod_number }}</span>
                    </a>
                </li>
            {% endif %}
        </ul>
        <div class="nav-item dropdown" style="margin-right: 70px;">
            <a class="nav-link dropdown-toggle custom-menu" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                {{ app.user.username }}
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="{{ path('bilan_sale_page') }}">Bilan</a>
                <a class="dropdown-item" href="{{ path('index_products_page') }}">Stock</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
            </div>
        </div>
    </div>
</nav>